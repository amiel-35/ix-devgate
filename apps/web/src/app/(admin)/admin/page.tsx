// Back-office — Dashboard
// Référence visuelle : docs/ds/mockups/devgate-backoffice.mockup.html
export default async function AdminDashboardPage() {
  // TODO: charger stats + tunnel health + activité récente via adminApi
  return (
    <main>
      {/* TODO: StatGrid + TunnelHealthTable + RecentAuditList */}
      <h1>Dashboard</h1>
    </main>
  );
}
