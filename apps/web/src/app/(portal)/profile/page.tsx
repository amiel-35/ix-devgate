// E12 — Profil / sessions actives
// Référence visuelle : docs/ds/mockups/devgate-e12-profile.mockup.html
export default async function ProfilePage() {
  // TODO: charger profil + sessions via portalApi.me() et portalApi.sessions()
  return (
    <main>
      {/* TODO: implémenter l'UI à partir du mockup E12 */}
      {/* ProfileCard + SessionList avec révocation */}
      <h1>Mon profil</h1>
    </main>
  );
}
